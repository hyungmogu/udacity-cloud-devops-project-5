apiVersion: v1
kind: Service
metadata:
  name: server
  labels:
    app: server
spec:
  type: NodePort
  ports:
    - name: "server"
      port: $SERVER_POD_PORT
      nodePort: $SERVER_SERVICE_NODE_PORT
  selector:
    app: server-main
