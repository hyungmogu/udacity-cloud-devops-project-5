pipeline {
    agent any
    environment {
        PUBLIC_KEY_PATH= "${env.PUBLIC_KEY_PATH}"
        DOCKER_IMAGE = "${env.DOCKER_USER_ID}/img-converter"
    }
    stages {
        stage('Lint') {
            echo "Hello World"
        }
    }
}